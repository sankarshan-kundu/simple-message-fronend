# simple-message-fronend
Simple message UI with Angular

## Build and run the Docker image

* `docker build -t simple-message-ui .` 
* `docker run -d -p 4000:4200 --name message-ui simple-message-ui` 
